{
  "name": "@agent-trail/typescript-sdk",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "bun run --parallel check:biome check:types test check:fallow",
    "check:biome": "biome check .",
    "check:fallow": "fallow --fail-on-issues --format compact",
    "check:types": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "@types/bun": "1.3.14",
    "fallow": "2.95.0",
    "typescript": "6.0.3"
  }
}
